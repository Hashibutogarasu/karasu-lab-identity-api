version: '3.8'

services:
  app:
    image: hashibutogarasu/karasu-lab-identity-api:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: ${DATABASE_URL}
    ports:
      - "3000:3000"
    env_file:
      - .env.production
